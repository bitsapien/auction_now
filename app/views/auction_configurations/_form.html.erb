<%= bootstrap_form_for(auction_configuration) do |f| %>
  <% if auction_configuration.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(auction_configuration.errors.count, "error") %> prohibited this auction_configuration from being saved:</h2>

      <ul>
      <% auction_configuration.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.text_field :application_name %>
  </div>

  <div class="field">
    <%= f.number_field :bid_increment_value %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
