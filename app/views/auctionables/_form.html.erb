<%= bootstrap_form_for(auctionable) do |f| %>
  <% if auctionable.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(auctionable.errors.count, "error") %> prohibited this auctionable from being saved:</h2>

      <ul>
      <% auctionable.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.text_field :name %>
  </div>

  <div class="field">
    <%= f.text_area :description %>
  </div>

  <div class="field">
    <%= f.text_field :image_url %>
  </div>

  <div class="field">
    <%= f.number_field :base_price %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
